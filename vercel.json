{
  "version": 2,
  "buildCommand": "cd frontend && yarn install --frozen-lockfile --network-timeout 600000 --ignore-engines && yarn build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && yarn install --frozen-lockfile --network-timeout 600000 --ignore-engines",
  "framework": "nextjs"
}
